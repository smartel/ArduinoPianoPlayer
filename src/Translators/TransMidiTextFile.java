package Translators;

public class TransMidiTextFile {

	// TODO reminder - it is not necessary to write rest notes that you translate (if they exist in the midi format),
	// since neither the gui nor the fingers need to perform an action to play a rest.
	
}
